<Window x:Class="CDArchive.App.Views.VariantEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Variant" Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <StackPanel Margin="16">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Description" VerticalAlignment="Center"
                       Foreground="#555" FontSize="12" Margin="0,0,8,0" />
            <TextBox   Grid.Row="0" Grid.Column="1" x:Name="DescriptionBox"
                       FontSize="13" Padding="4,3" Margin="0,3" />

            <TextBlock Grid.Row="1" Text="Long Description" VerticalAlignment="Top"
                       Foreground="#555" FontSize="12" Margin="0,6,8,0" />
            <TextBox   Grid.Row="1" Grid.Column="1" x:Name="LongDescriptionBox"
                       FontSize="13" Padding="4,3" Margin="0,6,0,0"
                       AcceptsReturn="True" TextWrapping="Wrap"
                       Height="100" VerticalScrollBarVisibility="Auto" />
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="OK" Width="80" Padding="0,6"
                    IsDefault="True" Click="OnOkClick" />
            <Button Content="Cancel" Width="80" Padding="0,6"
                    Margin="8,0,0,0" IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
